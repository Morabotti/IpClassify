export enum LocalStorageKey {
  Token = 'ip-c-token',
  ThemeMode = 'ip-c-theme',
  ThemeSchema = 'ip-c-schema'
}

export enum Client {
  GetTodo = 'todo'
}

export enum QueryParams {
  Rows = 'rows',
  Page = 'page',
  Redirect = 'redirect',
  Params = 'params',
  Order = 'order',
  OrderBy = 'orderBy'
}
